// Code that is shared between the two tasks assigned per day, but not needed elsewhere
// Functions that are generally useful are put in /common/aoccom.cpp

#include <vector>

int xkcdRand() {
    return 4; // Chosen by xkcd "random comic" feature
              // Guaranteed to be random
}
